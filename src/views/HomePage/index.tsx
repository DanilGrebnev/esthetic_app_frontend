import { PostsList } from '@/entities/posts'
import { Container } from '@/shared/ui/Container'

export const Home = () => {
    return (
        <Container>
            <PostsList />
        </Container>
    )
}
