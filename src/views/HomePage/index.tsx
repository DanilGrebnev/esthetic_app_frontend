import { Container } from '@/shared/ui/Container'
import { PostsList } from '@/widgets/PostsList'

export const Home = () => {
    return (
        <Container>
            <PostsList />
        </Container>
    )
}
