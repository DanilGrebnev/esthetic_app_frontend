'use client'

import s from './s.module.scss'

export const SelectDashboardModal = () => {
    return (
        <div className={s['full-window-container']}>
            <div className={s['list-container']}></div>
        </div>
    )
}
